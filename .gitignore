.DS_Store
__pycache__/
*.py[cod]

# Secrets and machine-local settings. Commit hub.yml.example; keep the live
# deployment manifest local because it may contain organization-specific state.
.env
/hub.yml

# Local planning and deployment-specific presets are not part of the generic
# public repository.
/TODO.md
/presets/

# Local CMS checkout used by the dev overlay. The deployed source comes from
# HUBZERO_REPO, never from this directory.
/hubzero-cms

# Database dumps (make backup / make restore).
*.sql
*.sql.gz

# Host-side full-state snapshots. These contain .env, uploads and TLS material.
/backups/
