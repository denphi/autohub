# The image contains no application source, so the build context only needs the
# docker/ directory plus project templates (baked in so cluster deployments,
# which have no bind mounts, can serve them). Everything else stays excluded --
# in particular .env must never be baked into an image.
*
!docker
!templates
